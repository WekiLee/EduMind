fastapi>=0.111,<1
uvicorn[standard]>=0.30
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
alembic>=1.13
redis>=5.1
httpx>=0.27
pydantic>=2.8
pydantic-settings>=2.3
email-validator>=2.0
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
bcrypt>=4.0.0,<5.0.0
python-multipart>=0.0.9
neo4j>=5.22
litellm>=1.44
pyyaml>=6.0
python-dotenv>=1.0

# 文档解析（可选）
unstructured[pdf,docx,pptx,md]>=0.15

# 语音
edge-tts>=6.0
numpy>=1.24
# kokoro>=0.9  # ONNX TTS（可选，替代 edge-tts，需额外安装）
# faster-whisper 可选（体积较大），首次使用需先安装：
# pip install faster-whisper

# 向量搜索（pgvector 字段支持为启动必需依赖）
pgvector>=0.3.0
# sentence-transformers>=3.0  # 本地嵌入模型（如不使用本地可跳过）

# 测试
pytest>=8.0
pytest-asyncio>=0.24
pytest-mock>=3.14
pytest-cov>=5.0
ruff>=0.8
mypy>=1.13
